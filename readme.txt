The main.py file conatains all the code necessary to make predictions based on the attributes excluding the target variable- Incident occured.
Two models were trained the XGB and the decision tree classifier and the my_model.pkl file is  pickled the XGB model.
#--------------------DATASET--------------------------------#
unzip the fflag folder in the same dir. (filesize ~700MB) 
